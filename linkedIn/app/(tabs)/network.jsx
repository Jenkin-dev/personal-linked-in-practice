import { SafeAreaView } from "react-native-safe-area-context";
import { View, Text } from "react-native";

const Network = () => {
  return (
    <SafeAreaView>
      <View>
        <Text>Welcome to the Network screen</Text>
      </View>
    </SafeAreaView>
  );
};
export default Network;